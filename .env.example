# =============================================================================
# RatoNet — Configuração (.env)
# Copie para .env e ajuste os valores conforme seu ambiente.
# =============================================================================

# --- Dashboard / API ---
DASHBOARD_HOST=0.0.0.0
DASHBOARD_PORT=8000
DASHBOARD_STATIC_DIR=.
# CORS — origens permitidas (separadas por vírgula). Vazio = permite tudo (*).
# Em produção, defina o domínio: CORS_ORIGINS=https://meu-dominio.com
CORS_ORIGINS=

# --- Banco de Dados ---
DB_PATH=ratonet.db
DB_AUTO_APPROVE=false

# --- Admin ---
# Token obrigatório em produção. Gere com: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=

# --- SRT ---
SRT_BASE_PORT=9000
SRT_LATENCY_MS=500
SRT_MAX_LINKS=4
SRT_PASSPHRASE=

# --- SRTLA (bonding BELABOX — opcional) ---
SRTLA_ENABLED=false
SRTLA_SEND_PORT=5000
SRTLA_REC_PORT=5001
SRTLA_BINARY_PATH=

# --- RTMP Relay ---
# Cada streamer configura seus destinos via API (PUT /api/me/destinations).
# As variáveis abaixo são fallback global (legado).
RTMP_PRIMARY_URL=
RTMP_SECONDARY_URL=

# --- OBS WebSocket ---
OBS_HOST=localhost
OBS_PORT=4455
OBS_PASSWORD=
OBS_SCENE_LIVE=LIVE
OBS_SCENE_BRB=BRB
OBS_FALLBACK_DELAY_S=3.0
OBS_RECOVERY_DELAY_S=5.0

# --- Field Agent ---
FIELD_SERVER_WS_URL=ws://localhost:8000/ws/field
FIELD_TELEMETRY_INTERVAL_S=1.0
FIELD_GPS_DEVICE=localhost:2947
FIELD_STARLINK_ADDR=192.168.100.1:9200
# Interfaces de rede para bonding (separadas por vírgula). Vazio = auto-detect.
FIELD_NETWORK_INTERFACES=

# --- Field Agent — Vídeo (opcional) ---
FIELD_VIDEO_DEVICE=/dev/video0
FIELD_VIDEO_BITRATE=4000k
FIELD_VIDEO_RESOLUTION=1920x1080
FIELD_VIDEO_CODEC=libx264

# --- Health Monitor ---
HEALTH_THRESHOLD_DEGRADED=70
HEALTH_THRESHOLD_CRITICAL=40
HEALTH_THRESHOLD_DOWN=10
HEALTH_CHECK_INTERVAL_S=2.0
